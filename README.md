# Pastebin clone using FastAPI

> No UI

### Features
- Ability to share paste using a unique id
- Password protect a new paste

### Start the app

```bash
pip install -r requirements.txt
uvicorn main:app
```